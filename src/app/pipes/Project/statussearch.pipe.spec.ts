import { StatussearchPipe } from './statussearch.pipe';

describe('StatussearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StatussearchPipe();
    expect(pipe).toBeTruthy();
  });
});
