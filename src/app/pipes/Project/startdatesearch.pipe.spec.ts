import { StartdatesearchPipe } from './startdatesearch.pipe';

describe('StartdatesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StartdatesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
