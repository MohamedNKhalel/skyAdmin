import { EnddatesearchPipe } from './enddatesearch.pipe';

describe('EnddatesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EnddatesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
